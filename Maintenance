<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>TIVO — Volvemos pronto</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@400;700;800;900&family=Plus+Jakarta+Sans:ital,wght@0,400;0,500;0,600;0,700;1,400;1,700&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{margin:0;padding:0;box-sizing:border-box;}
html,body{width:100%;height:100%;overflow:hidden;}
:root{
  --o:#F04E1A;
  --p:#9B6FD4;
  --pk:#E8477A;
  --l:#C4D400;
  --g:#4DBD74;
  --bl:#4DB8E8;
  --y:#F9C31F;
  --dark:#0E0804;
  --cream:#F8F4EC;
}
body{background:var(--cream);color:var(--dark);font-family:'Plus Jakarta Sans',sans-serif;cursor:none;display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:100vh;position:relative;overflow:hidden;}
a{text-decoration:none;color:inherit;}

/* CURSOR */
#cursor{position:fixed;z-index:9999;pointer-events:none;width:12px;height:12px;background:var(--o);border-radius:50%;transform:translate(-50%,-50%);transition:width .2s,height .2s,background .2s;mix-blend-mode:multiply;}
#cursor.hover{width:40px;height:40px;background:var(--l);}
#cur-ring{position:fixed;z-index:9998;pointer-events:none;width:30px;height:30px;border:1.5px solid rgba(240,78,26,.25);border-radius:50%;transform:translate(-50%,-50%);}

/* FONDO — bloques de color como en el brand */
.bg-grid{position:fixed;inset:0;display:grid;grid-template-columns:repeat(8,1fr);grid-template-rows:repeat(5,1fr);z-index:0;opacity:.06;pointer-events:none;}
.bgc{transition:opacity .4s;}
.bgc:nth-child(1){background:var(--o);}
.bgc:nth-child(2){background:var(--p);}
.bgc:nth-child(3){background:var(--l);}
.bgc:nth-child(4){background:var(--pk);}
.bgc:nth-child(5){background:var(--bl);}
.bgc:nth-child(6){background:var(--g);}
.bgc:nth-child(7){background:var(--y);}
.bgc:nth-child(8){background:var(--o);}
.bgc:nth-child(9){background:var(--l);}
.bgc:nth-child(10){background:var(--p);}
.bgc:nth-child(11){background:var(--pk);}
.bgc:nth-child(12){background:var(--y);}
.bgc:nth-child(13){background:var(--bl);}
.bgc:nth-child(14){background:var(--g);}
.bgc:nth-child(15){background:var(--o);}
.bgc:nth-child(16){background:var(--p);}
.bgc:nth-child(17){background:var(--pk);}
.bgc:nth-child(18){background:var(--l);}
.bgc:nth-child(19){background:var(--bl);}
.bgc:nth-child(20){background:var(--y);}
.bgc:nth-child(21){background:var(--g);}
.bgc:nth-child(22){background:var(--o);}
.bgc:nth-child(23){background:var(--p);}
.bgc:nth-child(24){background:var(--pk);}
.bgc:nth-child(25){background:var(--l);}
.bgc:nth-child(26){background:var(--bl);}
.bgc:nth-child(27){background:var(--y);}
.bgc:nth-child(28){background:var(--g);}
.bgc:nth-child(29){background:var(--o);}
.bgc:nth-child(30){background:var(--p);}
.bgc:nth-child(31){background:var(--pk);}
.bgc:nth-child(32){background:var(--l);}
.bgc:nth-child(33){background:var(--bl);}
.bgc:nth-child(34){background:var(--g);}
.bgc:nth-child(35){background:var(--y);}
.bgc:nth-child(36){background:var(--o);}
.bgc:nth-child(37){background:var(--p);}
.bgc:nth-child(38){background:var(--pk);}
.bgc:nth-child(39){background:var(--l);}
.bgc:nth-child(40){background:var(--bl);}

/* STRIP TOP */
.strip{position:fixed;top:0;left:0;right:0;height:5px;display:flex;z-index:10;}
.s1{background:var(--o);flex:2}.s2{background:var(--p);flex:2}.s3{background:var(--pk);flex:1.5}.s4{background:var(--l);flex:2}.s5{background:var(--bl);flex:1.5}.s6{background:var(--g);flex:1.5}.s7{background:var(--y);flex:1}

/* LOGO */
.logo{position:fixed;top:28px;left:50%;transform:translateX(-50%);display:flex;align-items:center;gap:9px;font-family:'Unbounded',sans-serif;font-size:16px;font-weight:900;letter-spacing:-.04em;z-index:10;}
.logo-icon{width:32px;height:32px;background:var(--o);border-radius:50%;display:flex;align-items:center;justify-content:center;animation:spin-slow 14s linear infinite;}
@keyframes spin-slow{to{transform:rotate(360deg);}}

/* CONTENIDO */
.content{position:relative;z-index:2;text-align:center;padding:0 24px;max-width:680px;}

/* BADGE animado */
.badge{display:inline-flex;align-items:center;gap:8px;background:var(--dark);color:var(--l);padding:7px 18px;border-radius:100px;font-size:10px;font-weight:900;letter-spacing:.18em;text-transform:uppercase;margin-bottom:32px;}
.bdot{width:7px;height:7px;border-radius:50%;background:var(--l);animation:pulse 1.8s ease-in-out infinite;}
@keyframes pulse{0%,100%{transform:scale(1);opacity:1;}50%{transform:scale(1.6);opacity:.5;}}

/* HEADLINE */
.headline{font-family:'Unbounded',sans-serif;font-size:clamp(2.4rem,6vw,5.5rem);font-weight:900;line-height:1.05;letter-spacing:-.05em;color:var(--dark);margin-bottom:10px;}
.headline .o{color:var(--o);}
.headline .p{color:var(--p);}
.headline .l{color:var(--l);}

/* TYPING LINE */
.typing-row{display:flex;align-items:center;justify-content:center;gap:0;margin-bottom:24px;}
.typing-pill{display:inline-flex;align-items:center;background:var(--pk);padding:6px 20px 8px;border-radius:10px;overflow:hidden;}
#typing{font-family:'Unbounded',sans-serif;font-size:clamp(1rem,2.5vw,1.8rem);font-weight:900;letter-spacing:-.04em;color:#fff;white-space:nowrap;}
.blink{display:inline-block;width:2px;height:1em;background:#fff;margin-left:3px;vertical-align:middle;animation:bl .7s step-end infinite;}
@keyframes bl{0%,100%{opacity:1;}50%{opacity:0;}}

/* SUBTEXT */
.sub{font-size:clamp(.9rem,1.2vw,1rem);line-height:1.8;color:rgba(14,8,4,.44);max-width:480px;margin:0 auto 36px;}
.sub b{color:var(--dark);}

/* CARDS de estado */
.cards{display:flex;gap:10px;justify-content:center;flex-wrap:wrap;margin-bottom:36px;}
.card{background:#fff;border-radius:14px;padding:14px 20px;display:flex;align-items:center;gap:10px;box-shadow:0 8px 28px rgba(14,8,4,.07);border:1.5px solid transparent;transition:all .3s cubic-bezier(.34,1.56,.64,1);}
.card:hover{transform:translateY(-5px);}
.card.co{border-color:var(--o);}.card.cp{border-color:var(--p);}.card.cl{border-color:var(--l);}
.card-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0;}
.do{background:var(--o);animation:pulse 2s ease-in-out infinite;}
.dp{background:var(--p);animation:pulse 2.3s ease-in-out infinite;}
.dl{background:var(--l);animation:pulse 2.6s ease-in-out infinite;}
.card-txt{font-size:11px;font-weight:800;color:var(--dark);letter-spacing:.02em;}

/* CTA */
.cta-row{display:flex;align-items:center;justify-content:center;gap:10px;flex-wrap:wrap;}
.btn-o{background:var(--o);color:#fff;padding:13px 30px;border-radius:100px;font-size:12px;font-weight:800;display:inline-flex;align-items:center;gap:8px;box-shadow:0 8px 24px rgba(240,78,26,.35);transition:all .3s cubic-bezier(.34,1.56,.64,1);letter-spacing:.01em;}
.btn-o:hover{background:var(--l);color:var(--dark);transform:scale(1.06) translateY(-2px);box-shadow:0 12px 32px rgba(196,212,0,.4);}
.btn-ghost{color:rgba(14,8,4,.4);font-size:12px;font-weight:700;padding:13px 22px;border-radius:100px;border:1.5px solid rgba(14,8,4,.1);display:inline-flex;align-items:center;gap:7px;transition:all .25s;}
.btn-ghost:hover{border-color:var(--p);color:var(--p);}

/* FOOTER */
.foot{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);font-size:9.5px;color:rgba(14,8,4,.2);font-weight:600;letter-spacing:.06em;white-space:nowrap;}

/* DECORATIVE asterisk flotante */
.deco-star{position:fixed;font-family:'Unbounded',sans-serif;font-weight:900;pointer-events:none;user-select:none;opacity:.055;animation:float-star linear infinite;}
@keyframes float-star{0%{transform:translateY(0) rotate(0deg);}100%{transform:translateY(-110vh) rotate(360deg);}}

/* RESPONSIVE */
@media(max-width:600px){
  .cards{flex-direction:column;align-items:center;}
  .cta-row{flex-direction:column;align-items:stretch;}
  .btn-o,.btn-ghost{justify-content:center;}
}
</style>
</head>
<body>
<div id="cursor"></div>
<div id="cur-ring"></div>

<svg style="display:none" xmlns="http://www.w3.org/2000/svg">
  <symbol id="tivo" viewBox="0 0 100 100">
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(0 50 50)"/>
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(60 50 50)"/>
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(120 50 50)"/>
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(180 50 50)"/>
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(240 50 50)"/>
    <rect x="46" y="5" width="9" height="30" rx="4.5" fill="currentColor" transform="rotate(300 50 50)"/>
    <rect x="18" y="45" width="36" height="9" rx="4.5" fill="currentColor"/>
    <rect x="18" y="45" width="9" height="36" rx="4.5" fill="currentColor"/>
    <rect x="15" y="42" width="16" height="9" rx="4.5" fill="currentColor" transform="rotate(45 23 46.5)"/>
  </symbol>
</svg>

<!-- STRIP TOP -->
<div class="strip"><div class="s1"></div><div class="s2"></div><div class="s3"></div><div class="s4"></div><div class="s5"></div><div class="s6"></div><div class="s7"></div></div>

<!-- BG GRID -->
<div class="bg-grid" id="bgGrid"></div>

<!-- LOGO -->
<div class="logo">
  <span class="logo-icon">
    <svg width="18" height="18" viewBox="0 0 100 100" color="#fff"><use href="#tivo"/></svg>
  </span>
  TIVO
</div>

<!-- MAIN CONTENT -->
<div class="content">
  <div class="badge"><span class="bdot"></span>En remodelación</div>

  <h1 class="headline">
    Algo <span class="o">grande</span><br>
    está en <span class="p">camino.</span>
  </h1>

  <div class="typing-row">
    <div class="typing-pill">
      <span id="typing"></span><span class="blink"></span>
    </div>
  </div>

  <p class="sub">
    Estamos construyendo algo que va a cambiar la forma en que percibes el diseño creativo.<br>
    <b>Volveremos pronto con una experiencia wow.</b>
  </p>

  <div class="cards">
    <div class="card co">
      <span class="card-dot do"></span>
      <span class="card-txt">Diseño en proceso</span>
    </div>
    <div class="card cp">
      <span class="card-dot dp"></span>
      <span class="card-txt">Estrategia activa</span>
    </div>
    <div class="card cl">
      <span class="card-dot dl"></span>
      <span class="card-txt">Lanzamiento pronto</span>
    </div>
  </div>

  <div class="cta-row">
    <a href="mailto:hola@wearetivo.com" class="btn-o">
      Escríbenos mientras
      <svg width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M7 17L17 7M7 7h10v10"/></svg>
    </a>
    <a href="https://instagram.com/wearetivo" target="_blank" class="btn-ghost">
      @wearetivo →
    </a>
  </div>
</div>

<div class="foot">© 2025 TIVO Studio — Bogotá, Colombia ✦</div>

<!-- DECORATIVE FLOATING STARS -->
<div id="stars"></div>

<script>
// CURSOR
const cur=document.getElementById('cursor'),ring=document.getElementById('cur-ring');
let mx=0,my=0,rx=0,ry=0;
document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;cur.style.left=mx+'px';cur.style.top=my+'px';});
(function rl(){rx+=(mx-rx)*.11;ry+=(my-ry)*.11;ring.style.left=rx+'px';ring.style.top=ry+'px';requestAnimationFrame(rl);})();
document.querySelectorAll('a,button').forEach(el=>{
  el.addEventListener('mouseenter',()=>cur.classList.add('hover'));
  el.addEventListener('mouseleave',()=>cur.classList.remove('hover'));
});

// BG GRID — fill 40 cells
const bg=document.getElementById('bgGrid');
for(let i=0;i<40;i++){const d=document.createElement('div');d.className='bgc';bg.appendChild(d);}

// TYPING
const phrases=['Más creatividad.','Más impacto.','Más identidad.','Una nueva experiencia.','Diseño con alma.','Resultados reales.'];
let pi=0,ci=0,del=false,paused=false;
const tel=document.getElementById('typing');
function type(){
  if(paused){setTimeout(type,2000);paused=false;return;}
  const ph=phrases[pi];
  if(!del){tel.textContent=ph.slice(0,++ci);if(ci===ph.length){del=true;paused=true;setTimeout(type,100);return;}setTimeout(type,65);}
  else{tel.textContent=ph.slice(0,--ci);if(ci===0){del=false;pi=(pi+1)%phrases.length;setTimeout(type,400);return;}setTimeout(type,38);}
}
type();

// FLOATING ASTERISKS
const colors=['#F04E1A','#9B6FD4','#C4D400','#E8477A','#4DB8E8','#4DBD74','#F9C31F'];
const sizes=[60,80,100,120,50,90];
const container=document.getElementById('stars');
for(let i=0;i<10;i++){
  const s=document.createElement('div');
  s.className='deco-star';
  const sz=sizes[i%sizes.length];
  const col=colors[i%colors.length];
  const left=Math.random()*100;
  const dur=12+Math.random()*18;
  const delay=-(Math.random()*dur);
  s.style.cssText=`left:${left}%;bottom:-${sz}px;font-size:${sz}px;color:${col};animation-duration:${dur}s;animation-delay:${delay}s;`;
  s.textContent='✦';
  container.appendChild(s);
}
</script>
</body>
</html>
